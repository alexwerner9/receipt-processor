parameters:
  - name: id
    in: path
    type: string
    required: true
responses:
  200:
    description: a JSON with the points
    schema:
      type: object
      properties:
        points:
          type: integer
          